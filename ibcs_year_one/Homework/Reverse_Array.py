#my code
newArray = []

def reverseArray(arr): #todo dequeue then push
    for outLoop in arr:
        for inLoop in arr:
            if arr == arr.empty:
                print("Array reversed")
                return newArray
            elif ( arr != arr.empty ):
                oldItem = arr[0]
                minusLast = arr.split[0]
                newArray.extend(oldItem)

reverseString("5", "8", "4", "2", "9")

#def thing(a)
#a = [1,2,3,4,5]
#reverse_a = a[::-1]


#Teachers code
def reverseArray(str):
    new_str = ""
    for i in string:
        new_str = i + new_str
    print (new_str)
    return new_str
